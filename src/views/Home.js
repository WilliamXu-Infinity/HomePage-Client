import ToggleButton from './Components/ToggleButton'

function Home() {
  return (
    <>
      <p>Home</p>
      <ToggleButton />
      <p>Welcom to my home page, still working on the home page, maybe working on the css later, just adding stuff and functionality into it so far</p>
    </>
  );
}

export default Home